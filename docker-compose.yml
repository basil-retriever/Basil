
services:
  basil-search:
    build: ./basil-search
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - API_HOST=0.0.0.0
      - API_PORT=8000
    volumes:
      - ./basil-search/data:/app/data
      - ./basil-search/chroma_db:/app/chroma_db
    command: ["python", "basil_search/pipeline.py", "--serve"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  scraper:
    build: ./basil-search
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./basil-search/data:/app/data
      - ./basil-search/chroma_db:/app/chroma_db
    profiles:
      - scraping
    command: ["python", "basil_search/pipeline.py", "--url", "${TARGET_URL:-https://example.com}", "--all"]

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./basil-search/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - basil-search
    restart: unless-stopped
    profiles:
      - production

volumes:
  data:
  chroma_db:
